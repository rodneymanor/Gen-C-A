"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getCreateLintRule = void 0;
exports.getPathSafeName = getPathSafeName;
/**
 * Tiny wrapped over the ESLint rule module type that ensures
 * there is a docs link to our ESLint plugin documentation page,
 * as well as improving type support.
 */
var getCreateLintRule = exports.getCreateLintRule = function getCreateLintRule(getRuleUrl) {
  return function (rule) {
    rule.meta.docs.url = getRuleUrl(rule.meta.name);
    return rule;
  };
};

/**
 * If it's a nested rule, ensure the url is clean and safe for urls, file paths, etc.
 */
function getPathSafeName(ruleName) {
  return ruleName.replace('/', '-');
}