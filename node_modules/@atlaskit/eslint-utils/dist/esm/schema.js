import { DEFAULT_IMPORT_SOURCES } from './is-supported-import';
export var importSources = {
  type: 'array',
  items: {
    type: 'string'
  },
  uniqueItems: true,
  default: DEFAULT_IMPORT_SOURCES
};